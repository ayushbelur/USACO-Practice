# USACO Practice Solutions

**This repository contains USACO practice problems from past years that I complete.**

*In this README file, I will show the logic of every problem completed.*

---

## 🥉 Bronze Division

| Problem Name | Contest | Solution Link | Logic / Explanation |
| :--- | :--- | :--- | :--- |
| **Air Cownditioning** | 2021 December | [View Code](./USACO_Bronze_2021_Dec_AirCownditioning.cpp) | *To do this problem, we can notice that for every cow/stall, there is a certain temperature change needed to get to the optimal temperature. To represent this, we can use an array (called `needed`) that stores the amount of change per stall. We can observe that we need to make the difference of every adjacent value in this array 0, as all values in the array `needed` have to become 0. We can call this array of the difference of adjacent values `diff`, where `diff[i] = abs(needed[i + 1] - needed[i])`. Every time we change a certain range by -1 or +1, starting from `i` to `j`, we see that `diff[i - 1]` and `diff[j]` get closer to 0. To say this, we know that when reducing or adding a segment, every number in that segment should be the same sign, to reduce the number of operations. If we don't include `needed[i - 1]` and `needed[j]`, that means they are opposite signs from the values in between them, which is why when changing the range, `diff[i - 1]` and `diff[j]` get closer to 0. Remembering that we need to make all values in the array `diff` = 0, and every time we change a range, we get 2 values closer to 0, and so the answer to this problem will just become the sum of all values of `diff` divided by 2.* |
| **Lonely Photo** | 2021 December | [View Code](./USACO_Bronze_2021_Dec_LonelyPhoto.cpp) | *To find the number of all the lonely photos, we can iterate across the array of `N` cows, and for each cow, see how many different photos will make that cow lonely. As only one cow per photo can be lonely for a lonely photo, by doing this, we make sure we aren't overcounting. To find the number of lonely photos for each cow, we first find the number of lonely photos we can make using that cow and all of its consecutive neighbors to the left. You can iterate to the left and see how many cows are different from the lonely cow. You can do the same for the right. We already have the number of lonely photos to just the left and right. We can notice that since there are only two breeds, the consecutive cows to the left and right must all be the same breed as each other. The result of any combination of the right and left cows will always end up as a lonely photo because of this. And so, the answer for the number of the lonely photos for that lonely cow is just the sum of the lonely photos to the left, the number of lonely photos to the right, and the number of combinations possible with the left and right cows. This can be represented as `left_photos[i] + right_photos[i] + (left_photos[i] + 1) * (right_photos[i] + 1)` for all `i` from 1 to `n`.* |
| **Cow College** | 2022 December | [View Code](./USACO_Bronze_2022_Dec_CowCollege.cpp) |  |
| **Hungry Cow** | 2023 February | [View Code](./USACO_Bronze_2023_Feb_HungryCow.cpp) | *For this problem, we note that Bessie can only eat when there are haybales in the barn. In this problem, `1 <= T <= 10^14`; seeing how big T is, we know to pass this problem, we must track every new haybale delivery. To simulate this problem, we can have a variable `current_haybales` which starts at 0 and tracks the number of haybales inside the barn at every new delivery. We can also use a variable `prev_day` to track the day of the delivery before the current one. For each delivery, we can calculate the number of haybales Bessie ate between the last one and this. The variable `prev_day` is useful here because it helps us calculate `days_passed = curr_day - prev_day`. Using the information we have now, the number of haybales Bessie eats between the last delivery and the current one is `min(days_passed, current_haybales)` where current_haybales is the number of haybales before adding the new delivery. This is because, if there aren't enough days to eat all the haybales, all of them can't be consumed. To keep on updating, we first add those consumed haybales to our total counter, we subtract those consumed haybales from our `current_haybales` but add the new delivery of haybales, and we set `prev_day` to `curr_day`. After finishing all of the deliveries, we must do one final step to get our answer. We have to account for after the last delivery, how many haybales Bessie can eat. Like before, this will just be `min(current_haybales, T - prev_day + 1)`. After adding this to our counter, we will have our answer.* |
| **Stamp Grid** | 2023 February | [View Code](./USACO_Bronze_2023_Feb_StampGrid.cpp) | *For this problem, it is very important to look at the constraints. `N`, the side length of the grid, is from `1 <= N <= 20`. This makes us realize that a brute force method should work in this case. We can try every spot for the top left corner of the stamp in the grid. If this spot fits within the boundaries, we check if it should be stamped down or not. To do this, we start with a boolean saying that this stamp is valid. If the stamp will stamp down a blank in a certain spot, the boolean will stay true no matter what that grid position should be, because this won't affect the grid negatively. If the stamp will stamp down a black in a certain spot where the grid should be black, this boolean still stays true, as it will help that spot. The last case, where the stamp shows black, but the grid should be blank, will change the boolean to false, because stamping down will make a negative permanent change. In other words, if `(grid[i + x -1][j + y - 1] == '.' && stamp[i][j] == '*')` where x and y are the coordinates of the top left of the stamp, this stamp is not valid, and we should skip it. We must also note that the problem says we can rotate the stamp 90 degrees as many times as we want, so for every top left position, we can rotate 4 times to cover all of the possibilities. If a stamp is valid, we can stamp it on a separate grid, which in the end we can compare to the actual target grid. If the two grids are the same, then the answer is `YES`, if not, `NO`.* |
| **Watching MooLoo** | 2023 February | [View Code](./USACO_Bronze_2023_Feb_WatchingMooLoo.cpp) | *In this problem, we can simulate buying new subscriptions or continuing subscriptions. First, we need to decide when to start a new subscription or continue an existing one. We can make a variable `last_day` that tracks the day before the current day that Bessie watched on Mooloo, and starts at `-1e16`, to make sure we start a new subscription in the beginning. Another variable, `diff` is represented by `curr_day - last_day`. We can see the decision to continue or create a new subscription depends on `diff` and `K`. We try to choose the minimum of the two options: `diff` (extending) or `K` (creating). In this way, we keep the cost as low as possible. If `diff <= K`, we should continue the subscription, and add `diff`. If `diff > K`, we should create a new subscription, and add this day, meaning the cost added is `K + 1`. After finishing the simulation, we have the answer.* |
| **Air Cownditioning II** | 2023 January | [View Code](./USACO_Bronze_2023_Jan_AirCownditiongII.cpp) | *In this problem, we notice that `N <= 20` and `M <= 10`. Since these constraints are very small, we can just do a  brute force on this problem. We can generate all possible sets using bitmasking for speed, and check which subsets work. To do this, we can use a difference array that captures the conditioning of different intervals. After prefixing this into another array `stalls`, `stalls` will store the cooling amount for each stall. All we have to do now is check if each stall satisfied the necessary cooling amount. If it did, we check if the cost is the lowest we have see so far, and is so, store it. We find the cost for each subset by adding the cost for the conditioners we choose. At the end, we can output our answer, which is the minimum cost that is valid.* |
| **Leaders** | 2023 January | [View Code](./USACO_Bronze_2023_Jan_Leaders.cpp) | *For this problem, we notice that for a valid pair, at least one of the leaders has to fit the requirement of its range containing all the cows of its breed. This is because, if both of the leader cows don't satisfy this condition, meaning that neither contains all the cows of its breed, then they have to contain each other, which is impossible for both of them to do. So, we have three cases: the leader cows both contain all the cows of their respective breeds, only the Guernsey leader contains all the cows of its breed, and only the Holstein leader contains all the cows of its breed. To solve these cases fast enough, we have to recognize that only the first cow of its breed can be the "real" leader of its breed, because other cows' ranges can't include the cows before them. To check if the first cows are really leaders, we have variables `last_g` and `last_h`, which we update when we see a cow of that breed when inputting. If the range of the first cow for each breed includes their respective last variable, then they are a "real" leader. If both of the first cows are real leaders, then we can add one to our total counter. Then, we try the second case, where only the Guernsey leader contains all the cows of its breed. We can go through all cows before it, and see if that cow is a Holstein, and if its range includes the Guernsey leader. If these two conditions are met, we add one to `total`, and continue. If we do this for the last case, we will have our answer, and output `total`.* |
| **Moo Operations** | 2023 January | [View Code](./USACO_Bronze_2023_Jan_MooOperations.cpp) | *In this problem, we can right away say that if a string has a size of less than 3, it is impossible, and we should output `-1`. For strings with over two characters, we might have to break down the problem. For each string, to make it the string `MOO`, it has to become three characters. This means the minimum number of operations will be `str.size() - 3`, because deleting is one operation. Also, the string we get after deletion has to be consecutive, because when deleting, we can only touch the first and last characters. So, we can go through every possible consecutive string of length 3 and look at the number of added operations we must do to get `MOO`. But first, with a string of length 3, we have to recognize the impossible cases. This is simply when the middle character is `M`, because you can never change the middle character without deleting the first or last character, which would give you too short of a string. After that, there are four cases of possible 3 letter strings: `MOO`, `MOM`, `OOO`, and `OOM`. For each of these possibilities, we can see that the number of added operations is 0, 1, 1, and 2 respectively. After going through each of these 3 character strings, the one with the least number of added operations will be the one we choose. If there is no 3 letter string that can become `MOO`, the answer is `-1`. Otherwise, the answer will be the minimum number of operations (`str.size() - 3`) plus the added operations. We output the answer and do this for all test cases.* |
| **Maximizing Productivity** | 2024 February | [View Code](./USACO_Bronze_2024_Feb_MaximizingProductivity.cpp) |  |
| **Milk Exchange** | 2024 February | [View Code](./USACO_Bronze_2024_Feb_MilkExchange.cpp) |  |
| **Palindrome Game** | 2024 February | [View Code](./USACO_Bronze_2024_Feb_PalindromeGame.cpp) |  |
| **It's Mooin' Time II** | 2025 January | [View Code](./USACO_Bronze_2025_Jan_ItsMooinTimeII.cpp) |  |
| **Hoof Paper Scissors Minus 1** | 2025 US Open | [View Code](./USACO_Bronze_2025_Open_HoofPaperScissorsMinus1.cpp) |  |

---

## 🥈 Silver Division

| Problem Name | Contest | Solution Link | Logic / Explanation |
| :--- | :--- | :--- | :--- |
| **Closest Cow Wins** | 2021 December | [View Code](./USACO_Silver_2021_Dec_ClosestCowWins.cpp) | *To solve this problem, we can divide the number line divided into different intervals based on the placement of Farmer Nhoj's cows. From this, we can see that for each interval, you only need two cows to claim the whole tastiness in the interval. However, placing one and saving another could be a better choice. We can try both ways in this problem. First, for each interval, we calculate the maximum tastiness a single cow can take. The maximum total area a cow can take in an interval is `length / 2`, where `length` is the size of the interval. So, to find the maximum tastiness, we can do a sliding window while making sure that the first and last patches don't exceed length / 2. We can store this value in an array or vector. To include the value of the second cow, we can just find the `total tastiness in the interval - one cow tastiness`. After storing all the values for all the intervals, we can account for the exceptions of the edge intervals, which only needs one cow to get the tastinees of the patches there. After this, we find the most valuable n cows in the array and add their tastiness, giving us the final answer.* |
| **Connecting Two Barns** | 2021 December | [View Code](./USACO_Silver_2021_Dec_ConnectingTwoBarns.cpp) | *For this problem, we can use DSU to find connected components. To find a path between barns `1` and `N`, we can use an intermediate field. For each intermediate field, or node, we find the closest value to (higher or lower) it inside the connected component of Barn 1 using `lower_bound` and subtracting iterators. We also have to find, from this intermediate field, the closest value in the connected component of Barn N. For every intermediate field, the cost will just be `(closest value in first component - intermediate field) + (closest_value in nth component - intermediate field)`. After going through all of the intermediate fields, including `1` and `N`, we find the minimum, which is our answer.* |
| **Convoluted Intervals** | 2021 December | [View Code](./USACO_Silver_2021_Dec_ConvolutedIntervals.cpp) | *For this problem, we can use a difference array with a size of `2 * M` to store the number of intervals currently active for a value of `k`. To do this without checking every pair of cows, we look at the map coordinates and count how many individual intervals start and end at each position using two vectors. We then use a nested loop to pair up every start coordinate `i` with every start coordinate `j`, and we multiply their counts to find how many total pairs start overlapping at `i + j`. We add that value in our difference array at `i + j` for the starts, and do the same thing with the ends but drop a negative value at `i + j + 1` to show where they stop. Finally, we loop across our difference array from `0 to 2 * M` while keeping a running sum variable `ans` that adds up the values to print out the final answer for each `k`.* |
| **Redistributing Gifts** | 2022 February | [View Code](./USACO_Silver_2022_Feb_RedistributingGifts.cpp) | *In this problem, we can use a directed graph in which a cow points to a gift they want. To implement this, when reading in the input, add a directed edge from cow `i` to every gift on their wishlist, until we get to their current gift, which we include but then stop. We can notice that a cow will only give up their gift if they get something better from the other cow. So, if there is a path that connects the two cows, then they should and can trade, because both cows will improve their gifts. To check if which pairs are connected, we run a Floyd Warshall algorithm. In the end, we go through the wishlist of every cow in order and see if `connected[gift][i] == true`. If not, continue on the wishlist. If this is correct, then output that gift. It is guarenteed that a cow will have a gift, becuase the last gift on the wishlist is the one they already have. After repeating this process `N` times, for all the cows, the problem if finished.* |
| **Robot Instructions** | 2022 February | [View Code](./USACO_Silver_2022_Feb_RobotInstructions.cpp) | *In this problem, we notice that `N <= 40`. That means N is too large to brute force this problem in a `2^n` solution. However, we can split the number of instructions by 2, and process the 2 halves separately, giving us a `2^(n/2)` solution, which is fast enough. We can solve this problem recursively and, for each instruction, try choosing it or not. After generating both halves, we put the possible positions that we can get to from each half, and see if they can pair together. Every time we find a pair, we can add one to our counter, and at the end, output the answer.* |
| **Cereal 2** | 2022 January | [View Code](./USACO_Silver_2022_Jan_Cereal2.cpp) | *For this problem, we can model a graph where the cereals are the nodes and the cows are the edges. The cows connect their first and second favorite cereals. We can first notice that there may be many components in this graph, and we can process them each separately. If there is no cycle in a specific connected component, no cows will get hungry. This is because, in this scenario, there is 1 more node than edge, meaning that one node(cereal) will be left. If there is one cycle, you are guaranteed to satisfy all the cows. This is because, in a cycle, there are at as many nodes(cereal) as edges(cows), so everyone can be satisfied. If there are many cycles, there will inevitably be more edges(cows) than nodes(cereal), leaving multiple cows hungry. After doing this for each connected component, you will have the number of hungry cows. For the order, in each connected component, you can find it by which order you process the nodes. The order for a tree component will be starting from a leaf node and moving inwards, to make sure every cow can get at least one box of cereal. In the one cycle component, you can start from any node in the cycle and `DFS` from there. Finally, in the multi-cycle scenario, you can also start from any node in any cycle, and `DFS` from there. It doesn't matter which connected component you process first, because as said before, they are separate. At the end, output the order and the number of hungry cows.* |
| **Cow Frisbee** | 2022 January | [View Code](./USACO_Silver_2022_Jan_CowFrisbee.cpp) | *Instead of doing an `N^2` solution with brute force, we can use a stack to simulate the necessary checks for pairs. We only need to check for pairs before we push a new element into the stack. Before pushing, we first do a cleanup process by popping values that won't be used again. These values won't be used again if this new height will block them from new values, because all the pairs before this new height have already been processed. As we are popping, we add the distance between these popped elements and the new element, because we know they are pairs. It may look wrong, because if we pop an element that was shorter than the element in front of it that was popped, it should have been blocked from making pairs. However, this stack is pretty much monotonic, because the height of each element will be less than the element processed before it, if we do this process from the start. After this, we add the distance between the element below our new element, because since the stack is monotonic, it is guaranteed they will be pairs. Then, we push the next element, and repeat this process. In the end, we output our total distance. By doing this, we keep the elements in the stack low, and our time complexity low, which is `O(N)`.* |
| **Searching For Soulmates** | 2022 January | [View Code](./USACO_Silver_2022_Jan_SearchingForSoulmates.cpp) | *In this problem, we try to find the minimum number of operations to get from one number to another. To do this optimally and fast, we can find the results of the operations from each cow as they make their way to one, and by doing this, we may find an intermediate node that will make the process cost less. However, when we trace the second cow's path backward, we reverse the standard operations: if the number is even, we divide it by 2, and if it is odd, we subtract 1. We store these steps to find where the two cows' paths can meet. We can then just iterate through the elements in the 2 vectors, in which we have stored the values after each operation from `cow1` and `cow2`. We can do an optimized check to find matching elements, which means that we can use that element as a shortcut to get one cow to its soulmate. After doing this and finding the minimum cost, we output that cost and continue to the next pair.* |
| **Bakery** | 2023 February | [View Code](./USACO_Silver_2023_Feb_Bakery.cpp) | *In this problem, we can use binary search to find the minimum number of moonies that will fit the time constraints. During this binary search, we call a check function to see if this number of moonies is sufficient. We focus on cookie time in the function, and the rest of the moonies just go to muffin time. We start with a lower bound and upper bound of cookie time. These limits show that we can't reduce cookie time or muffin time below one. We can make an inequality where x is the number of moonies given to reducing cookie time: `ai * (tc - x) + bi * (tm - moonies + x) <= ci`. We can try to isolate x here to get the equation `x * (bi - ai) <= time_left` where time_left is a combination of other parts. We can divide `time_left` by `bi - ai`, but depending on its sign, the `<=` could fli p to `>=`. So depending on that value, we could get a new upper_bound or lower_bound. After updating the upper and lower bounds, if `lower_bound <= upper_bound`, then this amount of moonies will work. From the binary search in main, we will get the minimum amount of moonies that works and output that.* |
| **Cow-Libi** | 2023 February | [View Code](./USACO_Silver_2023_Feb_Cow-Libi.cpp) | *For this problem, we see that we can't do an `O(N * G)` brute force, as `N <= 10^5` and `G <= 10^5`. So, we notice that it "will always be possible for a single cow to travel between all grazings". Since each cow has one alibi, we just have to check if it can reach that alibi, because it is guaranteed that it will be able to go to all grazing locations. So now, all we have to look at is if a cow makes it to the alibi from the grazing before it and from the alibi to the grazing after it. So, for each cow, we use the binary search `lower_bound` function to find the grazing after it. To get the grazing before it, we can just decrement the iterator `it--`. We handle the edge cases of the alibi happening at the very start or the very end by just checking the grazing next to it. If it can't make it to that grazing, then it is innocent. If an alibi happens between two grazings, we check if it can go from the previous grazing to the alibi and from the alibi to the next grazing. If it can't do this, it is innocent. To check if it can make one point to another, we use the distance formula. After doing this for all `N` cows, we can output how many innocent cows we have.* |
| **Moo Route II** | 2023 February | [View Code](./USACO_Silver_2023_Feb_MooRoute_II.cpp) | *For this problem, to simplify it, we can subtract the layover from the departure of each flight. We can sort the flights by departure time in ascending order, and push the latest departing flight every time with a quick `pop_back()`. At the start, to fill the queue, we push the latest flight until we find a flight that leaves before 0. We use a `BFS` like algorithm to at every step see if we have a new `time_arrived` for an airport and look at the available departing flights. After doing this, we will have the earliest possible time for each airport.* |
| **Find And Replace** | 2023 January | [View Code](./USACO_Silver_2023_Jan_FindAndReplace.cpp) |  |
| **Following Directions** | 2023 January | [View Code](./USACO_Silver_2023_Jan_FollowingDirections.cpp) |  |
| **Moo Route** | 2023 January | [View Code](./USACO_Silver_2023_Jan_MooRoute.cpp) |  |
